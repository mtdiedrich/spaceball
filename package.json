{
  "name": "my-map-project",
  "version": "1.0.0",
  "devDependencies": {
    "@types/google.maps": "^3.54.0",
    "@types/leaflet": "^1.9.0",
    "esbuild": "^0.19.0",
    "leaflet": "^1.9.4",
    "typescript": "^5.0.0"
  },
  "scripts": {
    "build": "esbuild src/map.ts --bundle --outfile=src/map.js --format=iife",
    "start": "npm run build && http-server"
  },
  "dependencies": {
    "@types/mapbox-gl": "^3.4.1",
    "mapbox-gl": "^3.16.0"
  }
}